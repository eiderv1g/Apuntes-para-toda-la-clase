# 💻 CLASE 01
## 📎 CÓDIGO
### DEFINICIONES
Usamos la etiqueta `<dl>` para crear listas de definiciones (Definition Lists)<br> 
•	`<dl>` → Contenedor de la lista de definiciones<br>
•	`<dt>` → Definition Term, término que se va a definir<br>
•	`<dd>` → Definition Description, descripción o definición del término

### TABLAS
```
<table border="1">
    <thead>
        </thead><tr>
            <th>ASIR</th>
            <th>DAW</th>
            <th>SMR</th>
            <th>HTML</th>
        </tr>
    </thead>

    <tbody>
        <tr>
            <td>Administración de Sistemas Informáticos en Red</td>
            <td>Desarrollo de Aplicaciones Web</td>
            <td>Soporte de Microinformçatica y Red</td>
            <td>Hyper Text Markup Language</td>
        </tr>
    </tbody>

    <tfoot>
        <tr>
            <td>Dato 01</td>
            <td>Dato 02</td>
            <td>Dato 03</td>
            <td>Dato 04</td>
        </tr>
    </tfoot>
</table>
```

- `<table>` → crea la tabla
- `<tr>` → table row, define una fila
- `<th>` → table header, define una celda de encabezado (negrita y centrada por defecto)
- `<td>` → table data, define una celda de datos normal
- `<thead>` → cabecera de la tabla
- `<tbody>` → cuerpo de la tabla
- `<tfoot>` → pie de tabla

**Atributos de tablas:**
- `border = "x"` → define el grosor del borde de la tabla.
- `colspan ="x"` → combina varias columnas
- `rowspan="x"` → combina varias filas

### INTRODUCCIÓN A FORMULARIOS

-  `<form>` → Contenedor del formulario<br>
· `action` → URL donde se envían los datos<br>
· `method` → Método de envío (GET o POST)

- `<label>` → Texto que describe cada campo
- `<input>` → Campo de entrada de datos
- `<textarea>` → Área de texto más grande
- `<button type="submit">` → Botón para enviar el formulario

## 📎 TAREAS
📌 **Reto tablitas**<br>
- Construye estas tablas:<br>
<img src= "img-resumenes/reto-tablas.JPG">

